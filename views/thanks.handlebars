<div class="parallax-container valign-wrapper">
    <div class="section no-pad-bot">
        <div class="container">
            <div class="row center">
                <h5 class="header col s12 light">Thank you!</h5>
                <p>{{countSigners }} {{#if_eq countSigners "1"}} person has {{else}} persons have {{/if_eq}} signed the petition!</p>
                <p>Click <a href="/signers">here</a> to see the list of signers.</p>
            </div>
        </div>
    </div>
    <div class="parallax"><img src="background2.jpg" alt="Unsplashed background img 2"></div>
</div>

{{#if signature}}
<div class="container">
    <div class="section">

        <div class="row">
            <div class="col s12 center">
                <h3><i class="mdi-content-send brown-text"></i></h3>
                <h4>Your signature</h4>
                <img src="{{signature}}" />


            </div>
        </div>

    </div>
</div>
{{/if}}
